name: dart_service_provider
description: An services dependency provider, like dependency inject, easy to learn and easy to use.
version: 1.0.1
repository: https://github.com/codelovercc/dart_service_provider

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
